TMDB_API_KEY=your_tmdb_api_key_here
JWT_SECRET=change_this_secret
MONGO_URI=mongodb+srv://movieuser:<password>@cluster0.alhog1e.mongodb.net/?appName=Cluster0

# Copy this file to `.env` and fill values.
# Docker Compose will read root `.env` for these variables.
# If your password has special characters like `#` or `%`, URL-encode them (e.g., `#` -> `%23`, `%` -> `%25`).